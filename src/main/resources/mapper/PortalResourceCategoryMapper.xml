<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.zygl.mapper.PortalResourceCategoryMapper">

    <!-- 通用结果映射 -->
    <resultMap id="BaseResultMap" type="org.example.zygl.entity.PortalResourceCategory">
        <id column="pk_id" property="pkId"/>
        <result column="type_uid" property="typeUid"/>
        <result column="type_name" property="typeName"/>
        <result column="parent_id" property="parentId"/>
        <result column="level" property="level"/>
        <result column="path" property="path"/>
        <result column="type" property="type"/>
        <result column="order" property="order"/>
        <result column="status" property="status"/>
        <result column="creator" property="creator"/>
        <result column="create_time" property="createTime"/>
        <result column="updater" property="updater"/>
        <result column="update_time" property="updateTime"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <!-- 通用列 -->
    <sql id="Base_Column_List">
        pk_id, type_uid, type_name, parent_id, level, path, type, `order`, status,
        creator, create_time, updater, update_time, tenant_id, deleted
    </sql>

    <!-- 按资源类型查询 -->
    <select id="selectByType" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM portal_resource_category
        WHERE deleted = 0
        <if test="type != null">
            AND type = #{type}
        </if>
        ORDER BY `order` ASC
    </select>

    <!-- 按父级ID查询子分类 -->
    <select id="selectByParentId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM portal_resource_category
        WHERE deleted = 0
        AND parent_id = #{parentId}
        ORDER BY `order` ASC
    </select>

    <!-- 按type_uid查询 -->
    <select id="selectByTypeUid" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM portal_resource_category
        WHERE deleted = 0
        AND type_uid = #{typeUid}
        LIMIT 1
    </select>

    <!-- 按名称模糊查询 -->
    <select id="selectByTypeName" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM portal_resource_category
        WHERE deleted = 0
        <if test="typeName != null and typeName != ''">
            AND type_name LIKE CONCAT('%', #{typeName}, '%')
        </if>
        ORDER BY `order` ASC
    </select>

    <!-- 条件分页查询 -->
    <select id="selectPageByCondition" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM portal_resource_category
        WHERE deleted = 0
        <if test="type != null">
            AND type = #{type}
        </if>
        <if test="typeName != null and typeName != ''">
            AND type_name LIKE CONCAT('%', #{typeName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY `order` ASC
    </select>

    <!-- 新增 -->
    <insert id="insertCategory" parameterType="org.example.zygl.entity.PortalResourceCategory" useGeneratedKeys="true" keyProperty="pkId">
        INSERT INTO portal_resource_category
        (type_uid, type_name, parent_id, level, path, type, `order`, status, creator, create_time, updater, update_time, tenant_id, deleted)
        VALUES
        (#{typeUid}, #{typeName}, #{parentId}, #{level}, #{path}, #{type}, #{order}, #{status}, #{creator}, #{createTime}, #{updater}, #{updateTime}, #{tenantId}, 0)
    </insert>

    <!-- 批量新增 -->
    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO portal_resource_category
        (type_uid, type_name, parent_id, level, path, type, `order`, status, creator, create_time, updater, update_time, tenant_id, deleted)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.typeUid}, #{item.typeName}, #{item.parentId}, #{item.level}, #{item.path}, #{item.type}, #{item.order}, #{item.status},
             #{item.creator}, #{item.createTime}, #{item.updater}, #{item.updateTime}, #{item.tenantId}, 0)
        </foreach>
    </insert>

    <!-- 更新 -->
    <update id="updateCategory" parameterType="org.example.zygl.entity.PortalResourceCategory">
        UPDATE portal_resource_category
        <set>
            <if test="typeUid != null">type_uid = #{typeUid},</if>
            <if test="typeName != null and typeName != ''">type_name = #{typeName},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="level != null">level = #{level},</if>
            <if test="path != null">path = #{path},</if>
            <if test="type != null">type = #{type},</if>
            <if test="order != null">`order` = #{order},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updater != null and updater != ''">updater = #{updater},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="tenantId != null">tenant_id = #{tenantId},</if>
        </set>
        WHERE pk_id = #{pkId} AND deleted = 0
    </update>

    <!-- 根据ID逻辑删除 -->
    <update id="deleteById">
        UPDATE portal_resource_category SET deleted = 1 WHERE pk_id = #{id}
    </update>

    <!-- 批量逻辑删除 -->
    <update id="deleteBatchByIds">
        UPDATE portal_resource_category SET deleted = 1
        WHERE pk_id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

</mapper>
